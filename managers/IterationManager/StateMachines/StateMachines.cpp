#include "StateMachines.h"

/**
 * @brief Start coordinator state machine
 * @details Coordinator state machine handles the iterator threads by modeling the data buffers and initialization data,
 * in order to allow parallel work distribution
 * 
 * @return unsigned int 
 */
unsigned int startCoordinatorSM()
{
    //Start coordinator thread
    //The adress of the set container for the calculated points must be provided
    //
    return 0;
}
unsigned int startIteratorSM()
{
    //Start an iterator thread
    //thread must have a coordinator state machine

    return 0;
}

